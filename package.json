{
  "name": "@daanrongen/neo4j-mcp",
  "version": "1.0.2",
  "description": "MCP server for Neo4j — run Cypher queries, explore schema, and inspect database info over stdio",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daanrongen/neo4j-mcp.git"
  },
  "keywords": [
    "mcp",
    "neo4j",
    "cypher",
    "graph",
    "model-context-protocol",
    "effect"
  ],
  "bin": {
    "neo4j-mcp": "./dist/main.js"
  },
  "main": "./dist/main.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "inspect": "DANGEROUSLY_OMIT_AUTH=true mcp-inspector bun src/main.ts",
    "build": "bun build src/main.ts --outfile dist/main.js --target bun",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "release:patch": "npm version patch",
    "release:minor": "npm version minor",
    "release:major": "npm version major",
    "postversion": "git push --follow-tags"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "effect": "^3.21.0",
    "neo4j-driver": "^5.28.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "bun-types": "latest",
    "lefthook": "^2.1.5",
    "typescript": "^5.8.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
